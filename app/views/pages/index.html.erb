<h1>Welcome to the Web Scraper/Web Generator!</h1>
    <ul>
        <% @pages.each do |page| %>
            <li>
				<%= link_to page.title, request.url.concat(page.link)  %>
            </li>
        <% end %>
    </ul>
